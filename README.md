# videosee

Android-only custom image and video viewer built with Kotlin, Jetpack Compose, and Media3.

## Build

```bash
./gradlew :app:testDebugUnitTest :app:assembleDebug
```

## Install Debug APK

```bash
adb install -r app/build/outputs/apk/debug/app-debug.apk
```
